#include "sort.h"

/**
 * counting_sort - Sorting using counting algorithms
 *
 * @array: The array to be sorted
 * @size: The number of elements in the array
 */
void counting_sort(int *array, size_t size)
{
	int max;
       size_ti, count, array1;

	for (i = 0; i < size; i++)

